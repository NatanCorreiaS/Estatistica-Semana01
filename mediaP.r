# media ponderada

notasGrupo = c(76,83,89)
pesosGrupo = c(14,25,16)

# numerador
num = notasGrupo * pesosGrupo

# denominador
den = sum(pesosGrupo)

# calculo da media ponderada
mediaPonderada = sum(num)/den

mediaPonderada